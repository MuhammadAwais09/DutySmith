import 'package:flutter/material.dart';

class AppColors {
  // Primary Dark Blue
  static const Color primaryDarkBlue = Color(0xFF003366);

  // Secondary Bright Blue/Cyan
  static const Color secondaryCyan = Color(0xFF00AEEF);

  // White
  static const Color white = Color(0xFFFFFFFF);

  // Grey (text/shadows)
  static const Color greyText = Color(0xFF333333);
}