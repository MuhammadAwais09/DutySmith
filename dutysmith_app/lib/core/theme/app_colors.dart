import 'package:flutter/material.dart';

class AppColors {
  // Primary Dark Blue (Deep Navy)
  // Main body/background, header bar, "Duty" text
  static const Color primaryDarkBlue = Color(0xFF0A2A4E);

  // Accent Bright Blue / Cyan (Vibrant Blue)
  // Highlights, gradients, "Smith" text, primary actions
  static const Color accentBrightBlue = Color(0xFF1F8FF4);

  // White
  // Backgrounds, icons, text on dark surfaces, shield monogram
  static const Color white = Color(0xFFFFFFFF);

  // Dark Grey / Black
  // Secondary text and less-emphasized elements
  static const Color darkGrey = Color(0xFF1A1A1A);
}
