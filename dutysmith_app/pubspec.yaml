name: dutysmith_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.0-290.4.beta # Compatible with your Dart SDK 3.10.0
  # Note: The Firebase packages below require a minimum Dart SDK of 3.2.0 and Flutter >=3.3.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # Keep this if you need localization
    sdk: flutter
  provider: ^6.1.2
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  firebase_messaging: ^16.0.4
  firebase_database: ^12.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo.png